-- TODO: add support for nested modules
-- module Test where
--   gam = 200
--   add5 = add 5

id x = x

fibo : Int -> Int

fibo n =
  if (lte n 1) then
    1
  else
    add (fibo (sub n 1)) (fibo (sub n 2))

main =
  let
    n = 5
    result = fibo n
  in id result

--
